#include <iostream>
using namespace std;
int main () {
    int A[5]={10, 20, 30, 40, 50}, B[5]={1, 2, 3, 4, 5}, C[5];
    int n=5, suma=0, i, prom;
    for (i=0; i<n; i++) {
        C[i]=A[i]+B[i];
    }
    for (i=0; i<n; i++) {
        cout<<C[i]<<" ";
    }
    return 0;
}