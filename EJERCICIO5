#include <iostream>
using namespace std;
int main () {
    int A[5]={10, 20, 30, 40, 50}, n=5, suma=0, i;
    for (i=0; i<n; i++) {
        suma=suma+A[i];
    }
    cout<<suma<<endl;
    return 0;
}